# Backend

This folder is the core functionality to retrieve data from the ozBargain application and send to frontend

# Running locally

To run this server locally, first install the packages after navigating into the server directory.

`npm install`

After installing packages you can then run the server by running

`npm run dev`

> This project was created by Vincent La and Michael Valenta.
